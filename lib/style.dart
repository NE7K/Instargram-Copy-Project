import 'package:flutter/material.dart';

var theme = ThemeData(
    appBarTheme: AppBarTheme(
        color: Colors.white
    ),
    bottomAppBarTheme: BottomAppBarTheme(
        color: Colors.white
    )
);

// var _name; _는 다른 파일에서 사용되지 않게 막아주는 기능